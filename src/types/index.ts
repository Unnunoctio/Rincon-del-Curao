export interface StoreInfo {
    code: string
    name: string
}

export interface ProductView {
    slug: string
    title: string
    image: string
    price: number
    bestPrice: number
    discount: number
    average: number
}
